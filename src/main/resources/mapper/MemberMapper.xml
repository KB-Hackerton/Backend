<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kb_hack.backend.domain.member.mapper.MemberMapper">

    <insert id="insertMember" parameterType="kb_hack.backend.domain.member.dto.MemberDTO"
            useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO member(member_email,password,kakao_id,member_name,status,help_count,badge)
        VALUES(#{memberEmail},#{password},#{kakaoId},#{memberName},#{status},#{helpCount},#{badge})
    </insert>
    <insert id="insertAuth" parameterType="kb_hack.backend.domain.member.dto.AuthDTO">
        INSERT INTO auth (member_id,auth)
        VALUES (#{memberId},#{auth})
    </insert>
    <delete id="deleteUser" parameterType="long">
        delete from member
        where member_id = #{memberId}
    </delete>

    <update id="updatePasswordByMemberEmail">
        UPDATE member
        SET password = #{newPassword}
        WHERE member_email =#{memberEmail}

    </update>
    <update id="updateMembernameByMemberId">
        UPDATE member
        SET member_name = #{memberName}
        WHERE member_id = #{memberId}
    </update>

    <select id="getMemberByEmail" resultType="kb_hack.backend.domain.member.domain.Member">
        SELECT * FROM member
        WHERE member_email = #{memberEmail}
    </select>

    <select id="getMemberByMemberId" resultType="kb_hack.backend.domain.member.domain.Member">
        SELECT * FROM member
        WHERE member_id = #{memberId}
    </select>

    <select id="getMemberIdByEmail"  parameterType="string" resultType="Long">
        select member_id from member
        where member_email=#{memberEmail}
    </select>


    <select id="getAllMemberId" resultType="long">
        SELECT m.member_id
        FROM member m
                 LEFT JOIN auth a ON m.member_id = a.member_id
        WHERE (a.auth IS NULL OR a.auth != 'ROLE_Admin')
    </select>
</mapper>
