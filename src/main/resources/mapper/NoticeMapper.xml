<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kb_hack.backend.domain.notice.mapper.NoticeMapper">

    <select id="getAllNotice" resultType="kb_hack.backend.domain.notice.dto.response.NoticeDTO">
        SELECT title , notice_id
        FROM notice
    </select>
    <select id="getNoticeDetail" resultType="kb_hack.backend.domain.notice.dto.response.NoticeDetailDTO">
        SELECT *
        FROM notice
        WHERE notice_id = #{noticeId}
    </select>
</mapper>
