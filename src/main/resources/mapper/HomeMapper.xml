<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kb_hack.backend.domain.home.mapper.HomeMapper">

    <select id="getRecentAnnounce" resultType="map">
        SELECT request_end_date, announce_title, announce_id
        FROM announce
        ORDER BY created_at DESC
            LIMIT 3;
    </select>

    <select id="getRecentFestival" resultType="map">
        SELECT festival_title, festival_id
        FROM festival
        ORDER BY created_at DESC
            LIMIT 3;
    </select>

    <select id="getRecentArticle" resultType="map">
        SELECT title, article_id
        FROM article
        ORDER BY created_at DESC
            LIMIT 3;
    </select>

</mapper>