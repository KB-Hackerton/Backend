<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kb_hack.backend.domain.favorite.mapper.FavoriteMapper">

    <insert id="insertFavorite">
        INSERT INTO member_favorite (announce_id, member_id, created_at)
        VALUES (#{announceId}, #{memberId}, NOW())
    </insert>

    <delete id="deleteFavorite">
        DELETE FROM member_favorite
        WHERE announce_id = #{announceId}
          AND member_id = #{memberId}
    </delete>
</mapper>
